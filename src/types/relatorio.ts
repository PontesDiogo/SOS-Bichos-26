export interface RelatorioCard {
  resumo: string;
  valor: number;
}

export interface RelatorioPorStatus {
  status: string;
  quantidade: number;
}

export interface RelatorioPorTipo {
  tipo: string;
  quantidade: number;
}